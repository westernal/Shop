import { Action } from "redux";
import { PLAYER_REQUESTED } from "../actions";

 const reducer = (state: any , action: Action) => {
    switch (action.type) {
        case PLAYER_REQUESTED:
            
            break;
    
        default:
            break;
    }
}

export default reducer